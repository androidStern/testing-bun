# WorkOS AuthKit Configuration
WORKOS_CLIENT_ID=client_your_client_id_here
WORKOS_API_KEY=sk_test_your_api_key_here
WORKOS_COOKIE_PASSWORD=your_secure_password_here_must_be_at_least_32_characters_long
WORKOS_REDIRECT_URI=http://localhost:3000/callback

# Convex Configuration
CONVEX_DEPLOYMENT=dev:your-deployment
VITE_CONVEX_URL=https://your-convex-deployment.convex.cloud
CONVEX_INTERNAL_SECRET=your_convex_internal_secret

# Typesense (direct browser search for admin UI)
VITE_TYPESENSE_URL=http://localhost:8108
VITE_TYPESENSE_SEARCH_KEY=your_search_only_key

# ============================================
# Scrape Pipeline Configuration
# ============================================

# FlareSolverr (for bypassing Cloudflare)
FLARESOLVERR_URL=http://localhost:8191/v1

# Typesense (server-side, full access)
TYPESENSE_API_KEY=your_typesense_api_key
TYPESENSE_URL=http://localhost:8108

# Redis (for job deduplication)
REDIS_URL=redis://localhost:6379

# Mapbox (for geocoding in deduplication)
MAPBOX_API_KEY=your_mapbox_api_key

# Convex HTTP API endpoint for pipeline
CONVEX_SITE_URL=https://your-convex-deployment.convex.site
CONVEX_PIPELINE_SECRET=your-pipeline-secret-must-match-convex-dashboard

# Inngest (for scrape pipeline)
# INNGEST_EVENT_KEY=
# INNGEST_SIGNING_KEY=
